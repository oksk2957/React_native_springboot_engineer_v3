# 수정계획안12 — ProgrammingScreen 탭 누락 + ProblemScreen 네트워크 문제 (2026-06-09)

## 1. 현재 상황 (Current State)
**작업 폴더**: `plan_미완료/`
**목표**: ProgrammingScreen(코드탭)의 이론/주관식 탭 구현 + ProblemScreen(문제탭) 네트워크 문제 해결

## 2. 우선순위 매트릭스

### 🔴 P0 (최우선순위): 블로커
**기준** (모두 만족):
- ❌ 시스템 핵심 기능 (인증, DB, API) 장애

### 🟡 P1 (차우선순위): 필수 기능
**기준** (하나 이상):
- ❌ 시스템 핵심 기능 (인증, DB, API) 장애
- ⚠️ 기능은 동작하나 예외 처리 누락

### 🟢 P2 (일반): 개선사항
**기준** (하나 이상):
- ✅ 코드 품질 (가독성, 중복 제거)
- ✅ 성능 최적화 (쿼리, 렌더링)
- ✅ 확장성 (미래 요구사항 대비)

## 3. Step 1: 미완료 항목 수집 (Collect)

| ID | 제목 | 심각도 | 근거 | 상태 |
|----|------|--------|------|------|
| #1 | ProgrammingScreen 이론/주관식 탭 누락 | P1 | `ProgrammingScreen.tsx:58` card/wrongNote만 구현, 이론/주관식 미구현 | ❌ 미해결 |
| #2 | ProblemScreen 네트워크 에러 핸들링 부족 | P1 | `ProblemScreen.tsx` 상세 에러 메시지 없음, 재시도 로직 부재 | ❌ 미해결 |
| #3 | 로그아웃→AuthScreen 미전환 (재발) | P1 | `authStore.ts:290-322`, `App.tsx:340` onAuthStateChange 리스너 부재 추정 | ❌ 미해결 |
| #4 | Google 로그인 400 (인프라) | P3 | OCI 서버 NTP 동기화 필요, 코드 수정 완료 | ❌ 미해결 |

## 4. 우선순위 분석 및 의존성

### 🟡 P1 #1: ProgrammingScreen 이론/주관식 탭 누락
**상세 분석**:
- 현재 ProgrammingScreen에는 `card`(플래시카드) + `wrongNote`(오답노트) 탭만 구현됨 (`ProgrammingScreen.tsx:58`)
- TheoryScreen에는 `flash`(플래시카드) + `subjective`(주관식 퀴즈) 탭이 있음
- **해결책**: ProgrammingScreen에 `theory`(이론 학습) + `subjective`(주관식 퀴즈) 탭 추가

**의존성**:
- 선행: 없음 (독립적 구현 가능)
- 후행: 없음

**완료 기준**:
- [ ] CodeScreen에 이론 탭 추가
- [ ] CodeScreen에 플래시카드 탭 추가
- [ ] CodeScreen에 오답노트 탭 추가
- [ ] 각 탭이 정상 동작 확인
- [ ] TypeScript 에러 없음

### 🟡 P1 #2: 문제탭 네트워크 문제
**상세 분석**:
- ProblemScreen.tsx에서 API 호출 실패 시 상세 에러 메시지 없음
- 2단계 API 호출 패턴 (메타 → 개별 문제)에서 중간 실패 시 처리 미흡
- CORS 설정 문제 가능성 (실제 기기 사용 시)
- 토큰 만료 시 자동 갱신 실패 가능성

**해결책**:
1. 에러 핸들링 개선 (네트워크 에러 vs 404 vs 500 구분)
2. 재시도 로직 추가
3. API Base URL 설정 검증
4. 토큰 상태 로깅 강화

**의존성**:
- 선행: #1 (CodeScreen 탭 구현 후 문제탭도 일관성 있게 수정)
- 후행: 없음

**완료 기준**:
- [ ] ProblemScreen.tsx 에러 핸들링 개선
- [ ] 상세 에러 메시지 표시 (네트워크/404/500 구분)
- [ ] 재시도 버튼 동작 확인
- [ ] API Base URL 설정 검증
- [ ] 토큰 상태 로깅 강화

### 🟡 P1 #3: 로그아웃→AuthScreen 미전환
**상세 분석**:
- `authStore.ts:290-322`에서 로그아웃 처리
- `App.tsx:340`에서 onAuthStateChange 리스너 부재 추정
- isLoggingOut 플래그 import 경로 문제 가능성

**해결책**:
1. App.tsx에 onAuthStateChange 리스너 추가
2. isLoggingOut 플래그 검증

**의존성**:
- 선행: 없음
- 후행: 없음

**완료 기준**:
- [ ] onAuthStateChange 리스너 추가
- [ ] 로그아웃 시 AuthScreen 전환 확인
- [ ] isLoggingOut 플래그 정상 동작 확인

### 🟡 P3 #4: Google 로그인 400
**상세 분석**:
- OCI 서버와 클라이언트 시계 차이 (43200초 = 12시간)
- 코드 수정 완료, 인프라 작업 대기

**해결책**: OCI 서버 NTP 동기화 (`sudo ntpdate -u pool.ntp.org`)

**의존성**:
- 선행: 없음
- 후행: #3 (로그아웃 문제 해결 후 Google 로그인도 확인)

**완료 기준**:
- [ ] OCI 서버 NTP 동기화 완료
- [ ] Google 로그인 400 에러 해결 확인

## 5. 실행 계획 (Execution Plan)

### Phase 1: ProgrammingScreen 탭 구현 (P1 #1)
**Step 1.1**: ProgrammingScreen.tsx에 탭 네비게이션 추가
- `theory` (이론 학습) + `subjective` (주관식 퀴즈) 탭 추가
- 기존 `card` + `wrongNote` 탭과 함께 4개 탭 구성

**Step 1.2**: 각 탭별 데이터 로딩 구현
- theory 탭: `problemService.getTheoryProblems('프로그래밍언어')` 사용
- subjective 탭: 주관식 문제 필터링

**Step 1.3**: 검증
- TypeScript 컴파일 확인
- 각 탭 정상 동작 확인

### Phase 2: ProblemScreen 네트워크 문제 해결 (P1 #2)
**Step 2.1**: ProblemScreen.tsx 에러 핸들링 개선
- try-catch 블록에서 에러 타입별 처리
- 네트워크 에러, 404, 500 구분 메시지 표시

**Step 2.2**: 재시도 로직 추가
- 에러 발생 시 재시도 버튼 활성화
- 자동 재시도 옵션 (선택적)

**Step 2.3**: API Base URL 검증
- 환경변수 설정 확인
- Platform별 기본값 검증

**Step 2.4**: 토큰 상태 로깅 강화
- 토큰 만료 시간 로깅
- 토큰 갱신 실패 시 상세 로그

### Phase 3: 로그아웃 문제 해결 (P1 #3)
**Step 3.1**: App.tsx에 onAuthStateChange 리스너 추가
- authStore에서 export된 onAuthStateChange 사용
- 로그아웃 시 AuthScreen으로 전환

**Step 3.2**: isLoggingOut 플래그 검증
- import 경로 확인
- 플래그 동작 검증

### Phase 4: Google 로그인 문제 해결 (P3 #4)
**Step 4.1**: OCI 서버 NTP 동기화
- `sudo ntpdate -u pool.ntp.org` 실행
- 시계 차이 확인

**Step 4.2**: Google 로그인 테스트
- 400 에러 해결 확인
- authToken 정상 발급 확인

## 6. Memory 갱신 계획

### 수정 중:
- [ ] 이 계획안의 체크리스트를 Memory에 반영

### 수정 후:
- [ ] `team/MEMORY.md`: 이슈 상태 업데이트 (#1, #2, #3, #4 ❌→✅)
- [ ] `fix_완료된수정/`: 수정 내용 요약 파일 생성
  - `2026-06-09-codescreen-tabs-added.md`
  - `2026-06-09-problemscreen-network-fix.md`
  - `2026-06-09-logout-authscreen-fix.md`
- [ ] `plan_미완료/`: 이 파일 → archive/completed/ 이동

### 검증:
- [ ] `grep "#1" memory/` → 모든 파일에서 ✅로 표시됨
- [ ] `grep "#2" memory/` → 모든 파일에서 ✅로 표시됨
- [ ] 옵시디언 그래프에서 broken link 0건

## 7. 제약조건 (Constraints)

- ✅ 최소 변경 원칙: 문제의 근본 원인만 수정
- ✅ P0는 3건 이하: 집중을 위해 우선순위严格控制
- ✅ 의존성 명시: 선행/후행 조건 반드시 표시
- ✅ 완료 기준 정량화: 체크리스트 형식
- ✅ 디버그 주석: `// DEBUG: [작업내용]` 형식
- ✅ 파일명 날짜 포함: `YYYY-MM-DD_설명.md`

## 8. 산출물 (Deliverables)

- [ ] CodeScreen.tsx 수정 (4개 탭 구현)
- [ ] ProblemScreen.tsx 수정 (에러 핸들링 개선)
- [ ] App.tsx 수정 (onAuthStateChange 리스너 추가)
- [ ] Memory 갱신 완료
- [ ] Memory 네트워크 검증 결과 (broken link 0건)

## 9. 검증 기준 (Verification)

- ✅ 모든 P0/P1 항목에 파일 경로:라인 번호 명시
- ✅ 모든 항목에 의존성 (선행/후행) 표시
- ✅ 모든 항목에 완료 기준 체크리스트 포함
- ✅ Memory 갱신 후 grep으로 일관성 확인
- ✅ 옵시디언 그래프에서 연결성 확인
