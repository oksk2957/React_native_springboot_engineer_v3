# 수정계획안19: 랭킹 SQL 컬럼명 오류 + DB 마이그레이션 최종 계획안

**작성일**: 2026-06-09 16:40  
**작성자**: Claude (OpenClaude)  
**우선순위**: 🔴 P0 (블로커)  
**예상 소요시간**: 30분

---

## 1. 현재 상황 (Current State)

### 🔴 새로운 런타임 에러 발견
```
ERROR: column plp.language does not exist
Position: 1655
File: MypageStatisticsMapper.xml
Query: selectWrongAnswerRanking
```

### 근본 원인
- **SQL 오류**: `plp.language` → 실제 컬럼명은 `plp.prog_language`
- **영향 범위**: `/api/statistics/wrong-answer-ranking` API 500 에러
- **발생 위치**: MypageStatisticsMapper.xml:158-207 (UNION ALL 3번째 브랜치)

### 관련 테이블 구조
```sql
programming_language_problems (실제 컬럼)
├─ id (bigserial)
├─ subject_id (int4)
├─ prog_language (varchar(64)) ← language가 아님!
├─ question (varchar(1000))
├─ answer (varchar(500))
├─ explanation (varchar(2000))
├─ difficulty (int4)
└─ is_ai_generated (bool)
```

---

## 2. 우선순위 매트릭스 (Priority Matrix)

| ID | 제목 | 심각도 | 근거 | 상태 |
|----|------|--------|------|------|
| #19-1 | plp.language → plp.prog_language 수정 | 🔴 P0 | MypageStatisticsMapper.xml:187,192 잘못된 컬럼명 | ❌ 미해결 |
| #19-2 | 전체 XML 스캔: 잘못된 컬럼명 참조 확인 | 🟡 P1 | 다른 XML에서도 동일한 오류 가능성 | ❌ 미확인 |
| #18-1 | DB 마이그레이션: option1~5 컬럼 추가 | 🔴 P0 | Supabase DDL 실행 필요 | ❌ 미해결 |
| #15-1 | 코드탭 UI: learning_card 구조 통합 | 🟡 P1 | 이론탭과 동일한 UI/DB 구조 | ❌ 미구현 |

---

## 3. 의존성 그래프 (Dependency Graph)

```
#19-1 (P0: SQL 수정) ─────┐
                          ├─→ #19-2 (P1: 전체 스캔)
                          │
#18-1 (P0: DB 마이그레이션) ─→ #15-1 (P1: UI 통합)
```

**선행 조건**:
- #19-1: 즉시 수정 가능 (XML 파일만 변경)
- #18-1: Supabase MCP 토큰 갱신 필요

**후행 작업**:
- #19-2: #19-1 완료 후 전수조사
- #15-1: #18-1 완료 후 UI 구현

---

## 4. 실행 계획 (4-Step Process)

### Step 1: 전체 XML 스캔 (10분)
**입력**: `backend/src/main/resources/mapper/*.xml` 전체  
**처리**:
1. `grep -r "programming_language_problems" mapper/` 실행
2. 모든 참조점에서 `plp.language` 사용 여부 확인
3. 파일:라인 번호 매핑

**출력**:
- 문제점 리스트 (파일, 라인, 오류 내용)
- 수정 대상 파일 목록

### Step 2: P0 SQL 수정 (10분)
**수정 파일**: `MypageStatisticsMapper.xml`

**변경 사항**:
```xml
<!-- Before (187, 192라인) -->
COALESCE(plp.language, '') AS language_name
GROUP BY plp.id, plp.language

<!-- After -->
COALESCE(plp.prog_language, '') AS language_name
GROUP BY plp.id, plp.prog_language
```

**DEBUG 주석**:
```xml
<!-- DEBUG: 2026-06-09 plp.language → plp.prog_language 수정 (#19-1) -->
```

### Step 3: 빌드/런타임 검증 (5분)
**검증 명령**:
```bash
cd backend
./mvnw clean compile
./mvnw spring-boot:run
curl http://localhost:8080/api/statistics/wrong-answer-ranking
```

**성공 기준**:
- ✅ 빌드 성공 (BUILD SUCCESS)
- ✅ HTTP 200 응답
- ✅ JSON 데이터 반환 (empty array여도 OK)

### Step 4: Memory 갱신 (5분)
**갱신 대상**:
1. `team/MEMORY.md`: #19-1 ✅ 업데이트
2. `fix_완료된수정/2026-06-09_ranking-sql-column-name-fix.md`: 수정 기록 생성
3. `plan_미완료/`: 이 파일 → `archive/completed/` 이동

**검증**:
```bash
grep -r "#19-1" memory/ → 모든 파일에서 ✅ 확인
```

---

## 5. 완료 기준 체크리스트 (Definition of Done)

- [ ] 전체 XML 스캔 완료 (문제점 리스트 작성)
- [ ] MypageStatisticsMapper.xml 수정 (plp.language → plp.prog_language)
- [ ] DEBUG 주석 추가
- [ ] Maven 빌드 성공
- [ ] 런타임 HTTP 200 확인
- [ ] Memory 갱신 완료 (team/fix/plan)
- [ ] broken link 0건 확인

---

## 6. 제약조건 (Constraints)

- ✅ 최소 변경 원칙: 컬럼명만 수정, 로직 변경 없음
- ✅ P0는 2건 이하: #19-1 (SQL) + #18-1 (DB)만 집중
- ✅ 의존성 명시: #19-1 → #19-2 순서
- ✅ 디버그 주석: `<!-- DEBUG: [작업내용] -->` 형식
- ✅ 파일명 날짜 포함: `2026-06-09_ranking-sql-column-name-fix.md`

---

## 7. 산출물 (Deliverables)

1. ✅ 수정된 `MypageStatisticsMapper.xml`
2. ✅ 전체 XML 스캔 결과 보고서
3. ✅ 빌드 성공 로그
4. ✅ 런타임 HTTP 200 응답
5. ✅ Memory 갱신 완료 (3개 파일)
6. ✅ 이 계획안 → `archive/completed/` 이동

---

## 8. 리스크 (Risks)

| 리스크 | 확률 | 영향 | 대응 |
|--------|------|------|------|
| 다른 XML에서도 동일한 오류 | 🟡 중간 | 🟡 중간 | Step 1 전수조사로 사전 발견 |
| Supabase MCP 토큰 만료 | 🔴 높음 | 🔴 높음 | #18-1 보류, #19-1만 먼저 처리 |
| 빌드 실패 | 🟢 낮음 | 🟢 낮음 | 에러 로그 분석 → 즉시 수정 |

---

## 9. 다음 단계 (Next Actions)

**즉시 실행**:
1. Step 1: 전체 XML 스캔 (Grep 도구 사용)
2. Step 2: MypageStatisticsMapper.xml 수정
3. Step 3: 빌드/런타임 검증
4. Step 4: Memory 갱신

**보류**:
- #18-1 (DB 마이그레이션): Supabase MCP 토큰 갱신 대기
- #15-1 (UI 통합): #18-1 완료 후 진행

---

## 10. 승인 (Approval)

**승인 대기**:
- [ ] 사용자 승인 후 Step 1 실행
- [ ] Step 3 완료 후 최종 확인

**락(Lock) 선언**:
- 🔒 [LOCK] MypageStatisticsMapper.xml (랭킹 SQL)
- 🔓 [NO-LOCK] ProblemQueryMapper.xml (다른 AI 수정 가능)
- 🔓 [NO-LOCK] ProgrammingScreen.tsx (다른 AI 수정 가능)

---

**버전**: v1.0  
**최종 수정**: 2026-06-09 16:40
