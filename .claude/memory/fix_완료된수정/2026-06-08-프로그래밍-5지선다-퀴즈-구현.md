# 미완료46 완료 보고서: 프로그래밍 5지선다 퀴즈 구현 (2026-06-08)

## 🎯 작업 목표
- 코드탭에서 프로그래밍 문제를 5지선다 형태로 풀 수 있도록 구현
- 이론탑 주관식 퀴즈 오답이 오답노트에 저장되도록 수정

## ✅ 완료된 작업

### 1. 백엔드 수정
- **ProgrammingLanguageProblem.java**: option1~5 필드 및 Builder 파라미터 추가
- **ProblemQueryMapper.xml**:
  - `validateAnswerProc`: SUBJECTIVE + PROGRAMMING_LANGUAGE 문제 유형 UNION ALL 추가
  - `selectTheoryCardsByCategory`: programming_language_problems 테이블 UNION ALL 추가
- **DataInitService.java**: 프로그래밍 5지선다 예시 문제 5개 생성 (C/Java/Python/SQL/자료구조)
- **DB Migration 005**: `programming_language_problems` 테이블에 option1~5 VARCHAR(500) 컬럼 추가

### 2. 프론트엔드 수정
- **TheoryScreen.tsx**:
  - 중복 import 제거 (`import { problemService }`)
  - `handleCheckAnswer` 함수를 async로 변경
  - 오답 시 `problemService.submitAnswer(currentCard.id, userAnswer, 'SUBJECTIVE')` 호출 추가
- **ProgrammingScreen.tsx**: 이미 5지선다 UI 구현됨 (shuffledOptions, selectedOption, showAnswer 상태)

### 3. 예시 문제 데이터 (5개)
1. C 언어: Undefined Behavior (x++ + ++x)
2. Java: String vs StringBuffer vs StringBuilder 차이
3. Python: 리스트 참조 할당 (y = x)
4. SQL: GROUP BY와 HAVING 절 차이
5. 자료구조: 스택(LIFO) vs 큐(FIFO) 차이

## 🔧 기술적 구현

### 백엔드 정답 검증 (UNION ALL 패턴)
```xml
<select id="validateAnswerProc" resultType="map">
    SELECT ... FROM problem p WHERE p.type = 'OBJECTIVE'
    UNION ALL
    SELECT ... FROM subjective_problems sp
    UNION ALL
    SELECT ... FROM programming_language_problems plp
    LIMIT 1
</select>
```

### 프론트엔드 오답 저장 흐름
```typescript
// TheoryScreen.tsx - handleCheckAnswer
if (possibleAnswers.some(ans => ans === userAnswer)) {
  showToast('정답입니다! 🎉', 'success');
  setShowAnswer(true);
} else {
  showToast('오답입니다. ✍️', 'error');
  try {
    await problemService.submitAnswer(currentCard.id, userAnswer, 'SUBJECTIVE');
  } catch (error) {
    console.error('[TheoryScreen] submitAnswer 실패:', error);
  }
}
```

## 📝 남은 작업
- 백엔드 기존 컴파일 에러 수정 필요 (Lombok getter 인식 문제 - 제 작업과 무관)
  - ProblemResponse.java, SubjectApiController.java, GoogleAuthController.java 등
- 백엔드 시작 후 통합 테스트:
  1. 이론탑 주관식 퀴즈 오답 → 오답노트 저장 확인
  2. 코드탭 5지선다 퀴즈 작동 확인
  3. 프로그래밍 문제 오답 → 오답노트 저장 확인

## 🔗 관련 파일
- Backend: `DataInitService.java`, `ProblemQueryMapper.xml`, `ProgrammingLanguageProblem.java`
- Frontend: `TheoryScreen.tsx`, `ProgrammingScreen.tsx`
- DB Migration: `005_add_option_columns_to_programming_problems.sql`
