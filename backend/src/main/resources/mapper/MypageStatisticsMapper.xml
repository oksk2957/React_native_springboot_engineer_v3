<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.informationexam.mapper.MypageStatisticsMapper">

    <select id="selectUserAnswerOverall" resultType="map">
        SELECT COUNT(*) AS attempted,
               COALESCE(SUM(CASE WHEN is_correct THEN 1 ELSE 0 END), 0) AS correct
        FROM user_answer
        WHERE user_id = #{userId}
    </select>

    <select id="selectBranchStats" resultType="com.example.informationexam.dto.statistics.MypageBranchStatRow">
        SELECT ua.item_type AS problemType,
               COUNT(*) AS attempted,
               COALESCE(SUM(CASE WHEN ua.is_correct THEN 1 ELSE 0 END), 0) AS correct
        FROM user_answer ua
        WHERE ua.user_id = #{userId}
        GROUP BY ua.item_type
    </select>

    <select id="selectObjectiveCategoryStats" resultType="com.example.informationexam.dto.statistics.ObjectiveCategoryStatRow">
        SELECT TRIM(s.name) AS category,
               COUNT(*) AS total,
               COALESCE(SUM(CASE WHEN ua.is_correct THEN 1 ELSE 0 END), 0) AS correct
        FROM user_answer ua
        INNER JOIN problem p ON ua.item_type = 'OBJECTIVE' AND ua.reference_id = p.id
        INNER JOIN subject s ON p.subject_id = s.id
        WHERE ua.user_id = #{userId}
        GROUP BY TRIM(s.name)
    </select>

    <select id="selectContentTotals" resultType="com.example.informationexam.dto.statistics.ContentTotalsRow">
        SELECT (SELECT COUNT(*) FROM problem) AS objective_count,
               (SELECT COUNT(*) FROM subjective_problems) AS subjective_count,
               (SELECT COUNT(*) FROM programming_language_problems) AS programming_count
    </select>

    <select id="selectWrongAnswers" resultType="com.example.informationexam.dto.statistics.WrongAnswerListRow">
        SELECT ua.id,
               ua.item_type AS problemType,
               ua.reference_id AS referenceId,
               p.question,
               p.answer AS correctAnswer,
               ua.submitted_answer AS submittedAnswer,
               ua.submitted_at AS submittedAt
        FROM user_answer ua
        INNER JOIN problem p ON ua.item_type = 'OBJECTIVE' AND ua.reference_id = p.id
        WHERE ua.user_id = #{userId}
          AND ua.is_correct = false
        UNION ALL
        SELECT ua.id,
               ua.item_type AS problemType,
               ua.reference_id AS referenceId,
               sp.question,
               sp.answer AS correctAnswer,
               ua.submitted_answer AS submittedAnswer,
               ua.submitted_at AS submittedAt
        FROM user_answer ua
        INNER JOIN subjective_problems sp ON ua.item_type = 'SUBJECTIVE' AND ua.reference_id = sp.id
        WHERE ua.user_id = #{userId}
          AND ua.is_correct = false
        UNION ALL
        SELECT ua.id,
               ua.item_type AS problemType,
               ua.reference_id AS referenceId,
               plp.question,
               plp.answer AS correctAnswer,
               ua.submitted_answer AS submittedAnswer,
               ua.submitted_at AS submittedAt
        FROM user_answer ua
        INNER JOIN programming_language_problems plp ON ua.item_type = 'PROGRAMMING_LANGUAGE' AND ua.reference_id = plp.id
        WHERE ua.user_id = #{userId}
          AND ua.is_correct = false
        ORDER BY submittedAt DESC, id DESC
    </select>
    <!-- DEBUG: [2026-06-07] 과목별 시도 횟수 랭킹 조회 - COUNT(ssi.id) 원본 숫자 그대로 반환 -->
    <select id="selectSubjectRanking" resultType="map">
        SELECT COALESCE(
                CASE WHEN ua.item_type = 'OBJECTIVE' THEN p.subject_id END,
                CASE WHEN ua.item_type = 'SUBJECTIVE' THEN sp.subject_id END,
                CASE WHEN ua.item_type = 'PROGRAMMING_LANGUAGE' THEN plp.subject_id END
               ) AS subjectId,
               COALESCE(
                CASE WHEN ua.item_type = 'OBJECTIVE' THEN TRIM(s.name) END,
                CASE WHEN ua.item_type = 'SUBJECTIVE' THEN TRIM(ss.name) END,
                CASE WHEN ua.item_type = 'PROGRAMMING_LANGUAGE' THEN TRIM(sls.name) END
               ) AS subjectName,
               COUNT(*) AS attemptedCount
        FROM user_answer ua
        LEFT JOIN problem p ON ua.item_type = 'OBJECTIVE' AND ua.reference_id = p.id
        LEFT JOIN subject s ON ua.item_type = 'OBJECTIVE' AND p.subject_id = s.id
        LEFT JOIN subjective_problems sp ON ua.item_type = 'SUBJECTIVE' AND ua.reference_id = sp.id
        LEFT JOIN subject ss ON ua.item_type = 'SUBJECTIVE' AND sp.subject_id = ss.id
        LEFT JOIN programming_language_problems plp ON ua.item_type = 'PROGRAMMING_LANGUAGE' AND ua.reference_id = plp.id
        LEFT JOIN subject sls ON ua.item_type = 'PROGRAMMING_LANGUAGE' AND plp.subject_id = sls.id
        WHERE ua.user_id = #{userId}
        GROUP BY COALESCE(
                    CASE WHEN ua.item_type = 'OBJECTIVE' THEN p.subject_id END,
                    CASE WHEN ua.item_type = 'SUBJECTIVE' THEN sp.subject_id END,
                    CASE WHEN ua.item_type = 'PROGRAMMING_LANGUAGE' THEN plp.subject_id END
                 ),
                 COALESCE(
                    CASE WHEN ua.item_type = 'OBJECTIVE' THEN TRIM(s.name) END,
                    CASE WHEN ua.item_type = 'SUBJECTIVE' THEN TRIM(ss.name) END,
                    CASE WHEN ua.item_type = 'PROGRAMMING_LANGUAGE' THEN TRIM(sls.name) END
                 )
        ORDER BY attemptedCount DESC
    </select>

    <!-- DEBUG: [2026-06-07] 날짜별 오답 조회 (Optional Auth 패턴 동기화) -->
    <select id="selectWrongAnswersByDate" resultType="com.example.informationexam.dto.statistics.WrongAnswerListRow">
        SELECT ua.id,
               ua.item_type AS problemType,
               ua.reference_id AS referenceId,
               p.question,
               p.answer AS correctAnswer,
               ua.submitted_answer AS submittedAnswer,
               ua.submitted_at AS submittedAt
        FROM user_answer ua
        INNER JOIN problem p ON ua.item_type = 'OBJECTIVE' AND ua.reference_id = p.id
        WHERE ua.user_id = #{userId}
          AND ua.is_correct = false
          AND DATE(ua.submitted_at) = #{date}
        UNION ALL
        SELECT ua.id,
               ua.item_type AS problemType,
               ua.reference_id AS referenceId,
               sp.question,
               sp.answer AS correctAnswer,
               ua.submitted_answer AS submittedAnswer,
               ua.submitted_at AS submittedAt
        FROM user_answer ua
        INNER JOIN subjective_problems sp ON ua.item_type = 'SUBJECTIVE' AND ua.reference_id = sp.id
        WHERE ua.user_id = #{userId}
          AND ua.is_correct = false
          AND DATE(ua.submitted_at) = #{date}
        UNION ALL
        SELECT ua.id,
               ua.item_type AS problemType,
               ua.reference_id AS referenceId,
               plp.question,
               plp.answer AS correctAnswer,
               ua.submitted_answer AS submittedAnswer,
               ua.submitted_at AS submittedAt
        FROM user_answer ua
        INNER JOIN programming_language_problems plp ON ua.item_type = 'PROGRAMMING_LANGUAGE' AND ua.reference_id = plp.id
        WHERE ua.user_id = #{userId}
          AND ua.is_correct = false
          AND DATE(ua.submitted_at) = #{date}
        ORDER BY submittedAt DESC, id DESC
    </select>

    <!-- DEBUG: [2026-06-09-수정계획안11] 문제별 오답 랭킹 (3가지 문제타입 UNION, 사용자별 필터링) -->
    <!-- DEBUG: [2026-06-09-수정계획안12] #78 해결 - questionText NULL 방지 (COALESCE) -->
    <select id="selectWrongAnswerRanking" resultType="com.example.informationexam.dto.statistics.WrongAnswerRankingRow">
        SELECT * FROM (
            SELECT
                p.id AS problem_id,
                'OBJECTIVE' AS item_type,
                p.id AS reference_id,
                TRIM(s.name) AS subject,
                COALESCE(NULLIF(TRIM(SUBSTRING(p.question, 1, 60)), ''), '(문제 없음)') AS question_text,
                COUNT(*) AS wrong_count
            FROM user_answer ua
            INNER JOIN problem p ON ua.reference_id = p.id
            INNER JOIN subject s ON p.subject_id = s.id
            WHERE ua.user_id = #{userId}
              AND ua.item_type = 'OBJECTIVE'
              AND ua.is_correct = false
            GROUP BY p.id, p.question, TRIM(s.name)

            UNION ALL

            SELECT
                sp.id AS problem_id,
                'SUBJECTIVE' AS item_type,
                sp.id AS reference_id,
                TRIM(ss.name) AS subject,
                COALESCE(NULLIF(TRIM(SUBSTRING(sp.question, 1, 60)), ''), '(주관식 문제)') AS question_text,
                COUNT(*) AS wrong_count
            FROM user_answer ua
            INNER JOIN subjective_problems sp ON ua.reference_id = sp.id
            INNER JOIN subject ss ON sp.subject_id = ss.id
            WHERE ua.user_id = #{userId}
              AND ua.item_type = 'SUBJECTIVE'
              AND ua.is_correct = false
            GROUP BY sp.id, sp.question, TRIM(ss.name)

            UNION ALL

            SELECT
                plp.id AS problem_id,
                'PROGRAMMING_LANGUAGE' AS item_type,
                plp.id AS reference_id,
                TRIM(sls.name) AS subject,
                COALESCE(NULLIF(TRIM(SUBSTRING(plp.question, 1, 60)), ''), '[프로그래밍] ' || COALESCE(plp.prog_language, '')) AS question_text,
                COUNT(*) AS wrong_count
            FROM user_answer ua
            INNER JOIN programming_language_problems plp ON ua.reference_id = plp.id
            INNER JOIN subject sls ON plp.subject_id = sls.id
            WHERE ua.user_id = #{userId}
              AND ua.item_type = 'PROGRAMMING_LANGUAGE'
              AND ua.is_correct = false
            GROUP BY plp.id, plp.question, plp.prog_language, TRIM(sls.name)
        ) combined
        ORDER BY wrong_count DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- DEBUG: [AI-AUTHOR-2026-06-09] 오답 달력 - 날짜별 오답 개수 조회 (wrong_answer_bookmark 기반) -->
    <select id="selectWrongAnswerCountByDateRange" resultType="map">
        SELECT DATE(bookmarked_at) AS date,
               COUNT(*) AS count
        FROM wrong_answer_bookmark
        WHERE user_id = #{userId}
          AND bookmarked_at >= #{startDate}
          AND bookmarked_at &lt; #{endDate}
        GROUP BY DATE(bookmarked_at)
        ORDER BY date
    </select>

</mapper>
