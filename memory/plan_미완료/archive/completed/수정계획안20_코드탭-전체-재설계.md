# 수정계획안20: 코드탭 전체 재설계 (2026-06-10)

**상태**: 📝 계획 작성 완료 → 승인 대기
**우선순위**: P1 (사용자 요청)

## 1. 배경 (Background)

### 사용자 요구사항
1. Supabase MCP로 테이블 구조 확인 → 코드탭을 이론탭과 동일하게 구성
2. 전체 화면 로딩바 → AJAX 형식 부분 로딩 (UX 개선)

### 현재 문제점 (Explore agent 조사 결과)

| 문제 | 위치 | 영향 |
|------|------|------|
| **파일명 스왑** | `ProgrammingScreen.tsx` ↔ `TheoryScreen.tsx` | import 경로 혼란 |
| **SQL 하드코딩** | `ProblemQueryMapper.xml:454-467` | `card_type='FLASHCARD'` + `NULL AS option1~5` → 주관식 불가 |
| **순차 로딩 4회** | `TheoryScreen.tsx:170-216` | C/Java/Python/공통 순차 호출 → 전체 로딩바 |
| **클라이언트 필터링 중복** | `TheoryScreen.tsx` LANGUAGE_KEYWORDS | 서버 필터링 + 프론트 필터링 충돌 가능 |

### DB 구조 (migration 파일 기준)
- `programming_language_problems`: option1~5 존재 (migration 005)
- `learning_card`: option1~5 + card_type 존재 (migration 006)

## 2. 목표 (Objectives)

1. **SQL 수정**: `NULL AS option1~5` → 실제 컬럼 참조, 동적 `card_type` 판별
2. **AJAX 로딩**: 전체 화면 로딩바 → 선택된 언어 카드만 부분 로딩
3. **TheoryScreen 패턴**: 코드탭도 이론탭과 동일한 UI/UX

## 3. 수정 계획 (Execution Plan)

### Step 1: SQL 수정 (백엔드)
**파일**: `backend/src/main/resources/mapper/ProblemQueryMapper.xml`

**변경 전** (lines 454-467):
```sql
SELECT id, 'FLASHCARD' AS card_type, question AS front_text, answer AS back_text,
       explanation, NULL AS option1, NULL AS option2, NULL AS option3, NULL AS option4, NULL AS option5,
       prog_language AS category, difficulty
FROM programming_language_problems
WHERE LOWER(prog_language) = LOWER(#{language})
ORDER BY id
```

**변경 후**:
```sql
SELECT id,
       CASE WHEN option1 IS NOT NULL AND option1 != '' THEN 'SUBJECTIVE' ELSE 'FLASHCARD' END AS card_type,
       question AS front_text, answer AS back_text, explanation,
       option1, option2, option3, option4, option5,
       prog_language AS category, difficulty
FROM programming_language_problems
WHERE LOWER(prog_language) = LOWER(#{language})
ORDER BY id
```

**⚠️ 전제 조건**: migration 005가 Supabase에 적용되어 `option1~5` 컬럼이 실제로 존재해야 함
- 미적용 시 → 500 에러 (`column pp.option1 does not exist`)
- 사용자 확인 필요: "Supabase DDL 적용 완료 여부"

### Step 2: 프론트엔드 재구현
**파일**: `InformationExamApp/src/screens/TheoryScreen.tsx` (ProgrammingScreen 컴포넌트)

**변경 포인트**:
1. **전체 로딩 제거**: `isLoading` 상태 제거, `isContentLoading`만 유지
2. **AJAX 패턴**: 언어 선택 시 → 해당 언어 카드만 fetch → 중간 콘텐츠 영역만 업데이트
3. **Subjective 탭**: option1~5 실제 데이터 기반 5지선다 UI (TheoryScreen 패턴)
4. **중복 필터링 제거**: `LANGUAGE_KEYWORDS` 삭제 (서버에서 이미 필터링)

**핵심 코드 변경**:
```tsx
// 변경 전: 전체 로딩
const [isLoading, setIsLoading] = useState(true);
const [isContentLoading, setIsContentLoading] = useState(false);

// 변경 후: 부분 로딩만
const [isContentLoading, setIsContentLoading] = useState(false);
const [currentLanguage, setCurrentLanguage] = useState('C');
const [cards, setCards] = useState<TheoryCard[]>([]);

// 언어 변경 시 AJAX fetch
const loadCards = async (lang: string) => {
  setIsContentLoading(true);
  const data = await fetchProgrammingCards(lang);
  setCards(data);
  setIsContentLoading(false);
};
```

### Step 3: 파일명 스왑 처리 (옵션)
**현재 상황**: 
- `ProgrammingScreen.tsx` → `TheoryScreen()` export
- `TheoryScreen.tsx` → `ProgrammingScreen()` export

**옵션 A**: 파일명 교환 (권장 — 일관성)
- `ProgrammingScreen.tsx` ↔ `TheoryScreen.tsx` rename
- `App.tsx` import 경로 수정

**옵션 B**: 그대로 유지 (최소 변경)
- 현재 상태 유지, 주석만 추가

**결정 필요**: 옵션 A vs B

## 4. 검증 단계 (Verification)

1. **SQL 검증**: `curl http://localhost:9001/api/problems/programming-theory?language=C` → option1~5 값 확인
2. **TypeScript 컴파일**: `cd InformationExamApp && npx tsc --noEmit`
3. **런타임 검증**: 앱 실행 → 코드탭 → C/Java/Python 선택 시 부분 로딩 확인
4. **주관식 퀴즈**: option 있는 문제 → 5지선다 UI 표시 확인

## 5. 리스크 및 블로커

| 리스크 | 영향 | 완화 |
|--------|------|------|
| **migration 005 미적용** | SQL 500 에러 | 사용자 DDL 확인 필수 |
| **파일명 스왑** | 혼란 | 옵션 A/B 결정 필요 |
| **러닝 데이터 부족** | 주관식 퀴즈 동작 안함 | migration 006으로 learning_card 복사됨 |

## 6. 잔존 이슈와의 의존성

- **수정계획안15 (이론탭 5지선다)**: 이론탭도 option1~5 하드코딩 → 실제 컬럼 전환 필요 (별도 수정)
- **Google 400 (P3)**: 무관, 인프라 작업 대기

## 7. 예상 소요 시간

- Step 1 (SQL): 10분
- Step 2 (프론트엔드): 20분  
- Step 3 (파일명): 5분 (옵션 A 선택 시)
- 검증: 10분
- **총계**: ~45분

---

## 승인 요청

**질문**:
1. **migration 005 DDL 적용 완료 여부**: "예" → 진행, "아니오" → DDL 실행 후 진행
2. **파일명 스왑**: 옵션 A (교환) vs 옵션 B (유지)
3. **수정 범위**: Step 1+2+3 전체 vs Step 1+2만

승인 후 실행하겠습니다.
