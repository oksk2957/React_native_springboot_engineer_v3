# 2026-05-06_PM_이론_탭_통합_실행_계획서

본 계획서는 이론 탭에 '운영체제', '네트워크', '데이터베이스', '소프트웨어공학', '정보보안', '애플리케이션테스트' 과목을 연동하고, `selectTheoryCardsByCategory` 쿼리를 활용하여 주관식/플래시 카드 학습 환경을 구축하기 위한 로드맵입니다.

## 1. 개요
- **목표**: 지정된 6개 과목에 대해 `subjective_problems`와 `learning_card` 데이터를 통합 조회하고, 카드형 인터페이스로 제공.
- **대상 과목**: 운영체제, 네트워크, 데이터베이스, 소프트웨어공학, 정보보안, 애플리케이션테스트

## 2. 세부 실행 단계

### [Step 1] 백엔드 매핑 계층 업데이트 (MyBatis & Interface)
- **Mapper XML**: `selectTheoryCardsByCategory` 쿼리 등록 및 기존 쿼리 정리.
- **Mapper Interface**: `ProblemQueryMapper.java`에 `selectTheoryCardsByCategory` 메서드 시그니처 추가.
- **Service Layer**: `ProblemService.java`에 이론 탭 카드 조회용 메서드(e.g., `getTheoryCards(String category)`) 구현.

### [Step 2] API 엔드포인트 구성
- `ProblemController.java`에 이론 탭 데이터 요청을 위한 신규 API 추가.
    - 예: `GET /problems/theory/cards?category={categoryName}`
- 응답 데이터 형식을 `ProblemResponseDto` 혹은 통합 DTO로 통일.

### [Step 3] 프론트엔드 연동 및 화면 전환
- **카테고리 매칭**: 각 과목 버튼 클릭 시 `category` 파라미터를 정확히 전달하도록 구성.
- **카드형 컴포넌트**: `LearningScreen.tsx` 또는 카드 전용 컴포넌트가 `card_type`을 구분하여 주관식/플래시 카드 인터페이스를 렌더링하도록 수정.
- **상태 관리**: 과목 선택 시 API를 호출하여 데이터를 캐싱하고, 슬라이드/뒤집기 인터페이스 활성화.

## 3. 검증 전략
- **쿼리 검증**: 디비버에서 6개 과목별로 쿼리 결과가 정상적으로 나오는지 확인.
- **API 검증**: API 호출 시 200 OK 및 카드 리스트 데이터(card_type 포함)가 정확히 오는지 확인.
- **UI 검증**: 실제 카드 UI가 데이터 타입에 맞춰(SUBJECTIVE/FLASHCARD) 작동하는지 확인.

---
**작성일**: 2026-05-06
**상태**: 실행 대기 (Ready to Execute)
