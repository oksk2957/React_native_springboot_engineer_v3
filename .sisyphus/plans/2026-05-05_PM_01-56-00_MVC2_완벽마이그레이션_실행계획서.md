# 2026-05-05_PM_01-56-00_MVC2_완벽마이그레이션_실행계획서
**프로젝트**: InformationExamProject  
**작성일**: 2026-05-05  
**모드**: Build (모든 MCP 사용 허가)  
**목표**: MVC2 패턴으로 완전 전환 - Controller → Mapper → DTO 구조 확립  

---

## 📋 사전 분석 요약 (확인 완료)

### 프로젝트 현재 상태
```yaml
백엔드: Spring Boot + MyBatis + JPA (혼용)
프론트엔드: React Native (Expo)
데이터베이스: PostgreSQL (Supabase)
현재 구조: Controller → Service → Mapper → DB
목표 구조: Controller → Mapper → DTO (MVC2)
```

### 주요 파일 위치
```text
backend/src/main/java/com/example/informationexam/
├── controller/ProblemApiController.java    # 리팩토링 대상
├── service/ProblemService.java             # 정리 대상
├── mapper/
│   ├── ProblemQueryMapper.java            # 유지 + 확장
│   ├── ProblemResponseAssembler.java      # 제거 대상
│   └── MypageStatisticsMapper.java        # 유지
├── dto/problem/
│   ├── ProblemResponseDto.java            # 수정: from() 메서드 추가
│   └── ProblemSqlRow.java                # 유지
└── resources/mapper/
    ├── ProblemQueryMapper.xml             # 최적화 대상
    └── MypageStatisticsMapper.xml        # 유지
```

---

## 🎯 MVC2 마이그레이션 실행 단계 (Step-by-Step)

### Phase 1: DTO 수정 - 정적 팩토리 메서드 추가
**목적**: Service 레이어 없이 DTO 자체에서 변환 처리

#### Step 1.1: ProblemResponseDto 수정
**파일**: `backend/src/main/java/com/example/informationexam/dto/problem/ProblemResponseDto.java`

**작업 내용**:
```java
// 기존 코드 유지 + 아래 메서드 추가
package com.example.informationexam.dto.problem;

import com.fasterxml.jackson.annotation.JsonProperty;
import lombok.*;
import java.util.LinkedHashMap;
import java.util.Map;

@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder
public class ProblemResponseDto {
    private Long id;
    private String question;
    @JsonProperty("correctAnswer")
    private String answer;
    private String explanation;
    private String category;
    private String type;
    private Integer difficulty;
    @JsonProperty("isAiGenerated")
    private Boolean isAiGenerated;
    private Map<String, String> options;
    private String programmingLanguage;

    // ★ MVC2 패턴용 정적 팩토리 메서드 추가
    public static ProblemResponseDto from(ProblemSqlRow row) {
        if (row == null) return null;
        Map<String, String> options = new LinkedHashMap<>();
        if ("OBJECTIVE".equals(row.getType())) {
            if (row.getOption1() != null && !row.getOption1().isBlank()) options.put("1", row.getOption1());
            if (row.getOption2() != null && !row.getOption2().isBlank()) options.put("2", row.getOption2());
            if (row.getOption3() != null && !row.getOption3().isBlank()) options.put("3", row.getOption3());
            if (row.getOption4() != null && !row.getOption4().isBlank()) options.put("4", row.getOption4());
            if (row.getOption5() != null && !row.getOption5().isBlank()) options.put("5", row.getOption5());
        }
        return ProblemResponseDto.builder()
                .id(row.getId())
                .question(row.getQuestion())
                .answer(row.getAnswer())
                .explanation(row.getExplanation())
                .category(row.getSubjectName() != null ? row.getSubjectName() : "")
                .type(row.getType())
                .difficulty(row.getDifficulty())
                .isAiGenerated(Boolean.TRUE.equals(row.getIsAiGenerated()))
                .options(options)
                .programmingLanguage(row.getType() != null && row.getType().equals("PROGRAMMING_LANGUAGE") ? row.getSubjectName() : null)
                .build();
    }

    // ★ 리스트 변환 유틸리티 메서드
    public static List<ProblemResponseDto> fromList(List<ProblemSqlRow> rows) {
        return rows == null ? List.of() : rows.stream()
                .map(ProblemResponseDto::from)
                .toList();
    }
}
```

**검증 방법**:
```bash
cd backend
./mvnw compile -q
```

---

### Phase 2: Controller 리팩토링 - Service 제거, Mapper 직접 호출
**목적**: MVC2 패턴 적용 - Controller가 Mapper 직접 호출

#### Step 2.1: ProblemApiController 수정
**파일**: `backend/src/main/java/com/example/informationexam/controller/ProblemApiController.java`

**작업 내용**:
```java
package com.example.informationexam.controller;

import com.example.informationexam.dto.problem.ProblemResponseDto;
import com.example.informationexam.dto.problem.TheoryProblemMetaDto;
import com.example.informationexam.mapper.ProblemQueryMapper;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

@RestController
@RequestMapping("/api/problems")
@RequiredArgsConstructor
@Slf4j
public class ProblemApiController {

    // ★ Service 대신 Mapper 직접 주입 (MVC2 핵심)
    private final ProblemQueryMapper problemQueryMapper;

    @GetMapping("/{id}")
    public ResponseEntity<ProblemResponseDto> getProblem(@PathVariable Long id) {
        log.info("[MVC2] 단건 문제 조회: id={}", id);
        ProblemResponseDto dto = ProblemResponseDto.from(problemQueryMapper.selectById(id));
        if (dto == null) {
            throw new IllegalArgumentException("해당 문제가 존재하지 않습니다. id=" + id);
        }
        return ResponseEntity.ok(dto);
    }

    @GetMapping("/{id}/{problemType}")
    public ResponseEntity<ProblemResponseDto> getProblemByType(
            @PathVariable Long id,
            @PathVariable String problemType) {
        log.info("[MVC2] 유형별 문제 조회: id={}, type={}", id, problemType);
        // 주관식/프로그래밍은 JPA 사용 (엔티티가 별도로 존재)
        throw new UnsupportedOperationException("해당 엔드포인트는 ProblemService를 통해 구현하세요.");
    }

    @GetMapping("/random/objective")
    public ResponseEntity<List<ProblemResponseDto>> getOneRandomProblemPerSubject() {
        log.info("[MVC2] 객관식 랜덤 문제 조회");
        List<ProblemResponseDto> dtos = ProblemResponseDto.fromList(
                problemQueryMapper.selectOneRandomProblemPerSubject("OBJECTIVE"));
        return ResponseEntity.ok(dtos);
    }

    @GetMapping("/theory")
    public ResponseEntity<List<ProblemResponseDto>> getTheoryProblems(@RequestParam String category) {
        log.info("[MVC2] 이론 문제 조회: category={}", category);
        List<ProblemResponseDto> dtos = ProblemResponseDto.fromList(
                problemQueryMapper.selectTheoryProblemsByCategory(category));
        return ResponseEntity.ok(dtos);
    }

    @GetMapping("/theory/meta")
    public ResponseEntity<TheoryProblemMetaDto> getTheoryProblemMeta(@RequestParam String category) {
        log.info("[MVC2] 이론 메타 조회: category={}", category);
        List<Long> ids = problemQueryMapper.selectTheoryProblemIdsByCategory(category);
        return ResponseEntity.ok(new TheoryProblemMetaDto(ids.size(), ids));
    }

    @GetMapping("/study/meta")
    public ResponseEntity<TheoryProblemMetaDto> getStudyProblemMeta(
            @RequestParam(defaultValue = "false") boolean randomSample,
            @RequestParam(required = false) com.example.informationexam.domain.problem.ProblemType type,
            @RequestParam(required = false) String category,
            @RequestParam(defaultValue = "0") int difficulty,
            @RequestParam(required = false) String excludeCategories,
            @RequestParam(defaultValue = "100") int limit) {
        log.info("[MVC2] 학습 메타 조회: randomSample={}, type={}, category={}", randomSample, type, category);
        
        List<Long> ids;
        if (randomSample) {
            List<String> excluded = (excludeCategories == null || excludeCategories.isBlank())
                    ? List.of()
                    : java.util.Arrays.stream(excludeCategories.split(","))
                            .map(String::trim)
                            .filter(s -> !s.isBlank())
                            .toList();
            String nextCategory = problemQueryMapper.selectRandomCategoryName(excluded);
            if (nextCategory == null || nextCategory.isBlank()) {
                return ResponseEntity.ok(new TheoryProblemMetaDto(0, List.of()));
            }
            ids = problemQueryMapper.selectRandomProblemIdsByCategory(nextCategory, limit);
        } else if (category != null && !category.isBlank()) {
            ids = problemQueryMapper.selectStudyIdsByDifficultyCategory(difficulty, category, limit);
        } else if (type != null) {
            ids = problemQueryMapper.selectStudyIdsByType(type.name(), limit);
        } else {
            throw new IllegalArgumentException("randomSample 또는 category 또는 type 중 하나는 필수입니다.");
        }
        return ResponseEntity.ok(new TheoryProblemMetaDto(ids.size(), ids));
    }

    @GetMapping
    public ResponseEntity<List<ProblemResponseDto>> getProblems(
            @RequestParam(required = false) String category,
            @RequestParam(required = false) com.example.informationexam.domain.problem.ProblemType type,
            @RequestParam(defaultValue = "1000") int limit) {
        if (limit <= 0) limit = Integer.MAX_VALUE;
        if (type != null) {
            return ResponseEntity.ok(ProblemResponseDto.fromList(
                    problemQueryMapper.selectRandomProblemsByType(type.name(), limit)));
        }
        return ResponseEntity.ok(ProblemResponseDto.fromList(
                problemQueryMapper.selectByDifficultyAndCategory(0, category, limit)));
    }

    @GetMapping("/subjective")
    public ResponseEntity<List<ProblemResponseDto>> getSubjectiveProblems(
            @RequestParam(required = false) Long subjectId,
            @RequestParam(defaultValue = "1000") int limit) {
        log.info("[MVC2] 주관식 문제는 /api/subjective-problems 엔드포인트를 사용하세요.");
        throw new UnsupportedOperationException("주관식 문제는 SubjectiveProblemController를 사용하세요.");
    }

    @GetMapping("/programming")
    public ResponseEntity<List<ProblemResponseDto>> getProgrammingProblems(
            @RequestParam(required = false) Long subjectId,
            @RequestParam(required = false) String language,
            @RequestParam(defaultValue = "1000") int limit) {
        log.info("[MVC2] 프로그래밍 문제는 /api/programming-problems 엔드포인트를 사용하세요.");
        throw new UnsupportedOperationException("프로그래밍 문제는 ProgrammingProblemController를 사용하세요.");
    }

    @GetMapping("/test")
    public ResponseEntity<Map<String, Object>> testConnection() {
        Map<String, Object> result = new HashMap<>();
        try {
            result.put("status", "ok");
            result.put("problemCount", problemQueryMapper.countAll());
            return ResponseEntity.ok(result);
        } catch (Exception e) {
            result.put("status", "error");
            result.put("message", e.getMessage());
            return ResponseEntity.status(500).body(result);
        }
    }

    @GetMapping("/debug/objective")
    public ResponseEntity<Map<String, Object>> getObjectiveProblemsDebug() {
        Map<String, Object> result = new HashMap<>();
        try {
            List<ProblemResponseDto> problems = ProblemResponseDto.fromList(
                    problemQueryMapper.selectRandomProblemsByType("OBJECTIVE", 1000));
            result.put("status", "ok");
            result.put("count", problems.size());
            result.put("problems", problems);
            return ResponseEntity.ok(result);
        } catch (Exception e) {
            result.put("status", "error");
            result.put("message", e.getMessage());
            return ResponseEntity.status(500).body(result);
        }
    }
}
```

**검증 방법**:
```bash
cd backend
./mvnw compile -q
```

---

### Phase 3: ProblemQueryMapper.xml 최적화
**목적**: PostgreSQL 최적화 + 비즈니스 로직 SQL 활용

#### Step 3.1: ProblemQueryMapper.xml 검토
**현재 상태**: 이미 PostgreSQL 문법 사용 중 (`random()`, `TRIM(COALESCE(...))`)

**최적화 내용** (필요 시 적용):
```xml
<!-- 기존 유지 + 인덱스 힌트 추가 (선택사항) -->
<select id="selectById" resultType="com.example.informationexam.dto.problem.ProblemSqlRow">
    SELECT <include refid="problemJoinedColumns"/>
    FROM problem p
    INNER JOIN subject s ON p.subject_id = s.id
    WHERE p.id = #{id}
    LIMIT 1
</select>
```

**검증 방법**:
```bash
# 애플리케이션 실행 테스트
cd backend
./mvnw spring-boot:run &
sleep 10
curl http://localhost:8088/api/problems/test
```

---

### Phase 4: Service 레이어 정리
**목적**: 불필요한 Service 메서드 제거, JPA 필요한 부분만 유지

#### Step 4.1: ProblemService 간소화
**파일**: `backend/src/main/java/com/example/informationexam/service/ProblemService.java`

**작업 내용**:
```java
package com.example.informationexam.service;

import com.example.informationexam.domain.problem.*;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * MVC2 패턴 적용 후: JPA 엔티티가 필요한 작업만 담당
 * - 주관식/프로그래밍 문제 조회 (별도 엔티티 존재)
 * - 답변 채점 (엔티티 접근 필요)
 */
@Service
@Transactional(readOnly = true)
@RequiredArgsConstructor
public class ProblemService {

    private final ProblemRepository problemRepository;
    private final SubjectiveProblemRepository subjectiveProblemRepository;
    private final ProgrammingLanguageProblemRepository programmingLanguageProblemRepository;

    /**
     * 답변 검증: JPA 엔티티 접근 필요
     */
    public boolean checkAnswer(Long problemId, String submittedAnswer) {
        Problem problem = problemRepository.findById(problemId)
                .orElseThrow(() -> new IllegalArgumentException("해당 문제가 존재하지 않습니다. id=" + problemId));
        return problem.getAnswer().equalsIgnoreCase(submittedAnswer);
    }

    /**
     * 문제 엔티티 조회 (AnswerService에서 사용)
     */
    public Problem getProblemEntity(Long id) {
        return problemRepository.findById(id)
                .orElseThrow(() -> new IllegalArgumentException("해당 문제가 존재하지 않습니다. id=" + id));
    }

    // ★ 아래 메서드들은 Controller로 이관됨 - 삭제 대상
    // getOneRandomProblemPerSubject(), getProblemsByType(), getTheoryProblems() 등
    // → ProblemApiController에서 Mapper 직접 호출하도록 변경됨
}
```

#### Step 4.2: ProblemResponseAssembler 제거
**파일**: `backend/src/main/java/com/example/informationexam/mapper/ProblemResponseAssembler.java`

**작업**: 파일 삭제
```bash
rm backend/src/main/java/com/example/informationexam/mapper/ProblemResponseAssembler.java
```

---

### Phase 5: PostgreSQL Stored Procedure 적용 (선택사항)
**목적**: 복잡한 비즈니스 로직을 DB 레벨로 이관

#### Step 5.1: Stored Procedure 생성 스크립트 작성
**파일**: `backend/src/main/resources/db/migration/V001__create_stored_procedures.sql`

**작업 내용**:
```sql
-- 사용자 통계 조회 함수
CREATE OR REPLACE FUNCTION get_user_statistics(p_user_id BIGINT)
RETURNS TABLE(
    total_problems BIGINT,
    solved_problems BIGINT,
    correct_count BIGINT,
    wrong_count BIGINT
) AS $$
BEGIN
    RETURN QUERY
    SELECT 
        (SELECT COUNT(*) FROM problem)::BIGINT,
        (SELECT COUNT(DISTINCT reference_id) FROM user_answer WHERE user_id = p_user_id)::BIGINT,
        (SELECT COUNT(*) FROM user_answer WHERE user_id = p_user_id AND is_correct = TRUE)::BIGINT,
        (SELECT COUNT(*) FROM user_answer WHERE user_id = p_user_id AND is_correct = FALSE)::BIGINT;
END;
$$ LANGUAGE plpgsql;

-- 문제 답변 검증 함수 (간단 버전)
CREATE OR REPLACE FUNCTION validate_answer(p_problem_id BIGINT, p_submitted_answer TEXT)
RETURNS TABLE(is_correct BOOLEAN, explanation TEXT, correct_answer TEXT) AS $$
BEGIN
    RETURN QUERY
    SELECT 
        UPPER(TRIM(answer)) = UPPER(TRIM(p_submitted_answer)),
        explanation,
        answer
    FROM problem 
    WHERE id = p_problem_id;
END;
$$ LANGUAGE plpgsql;
```

**실행 방법**:
```bash
# Supabase SQL Editor에서 직접 실행하거나
# psql 명령어 사용
psql "postgresql://postgres.gmhznnwecujoafdisscl:wjdcjrlgkqrur@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require" -f backend/src/main/resources/db/migration/V001__create_stored_procedures.sql
```

---

### Phase 6: 프론트엔드 호환성 검증
**목적**: API 응답 형식 변경 없음 확인

#### Step 6.1: API 응답 필드 확인
**필요 조건**:
- `correctAnswer` (camelCase JSON 필드명)
- `isAiGenerated` (camelCase JSON 필드명)
- `options` (Map<String, String>)

**검증 API**:
```bash
# 기본 연결 테스트
curl -s http://localhost:8088/api/problems/test | jq .

# 단건 조회 테스트
curl -s http://localhost:8088/api/problems/1 | jq '.correctAnswer, .isAiGenerated, .options'

# 랜덤 문제 테스트
curl -s http://localhost:8088/api/problems/random/objective | jq '.[0] | keys'

# 이론 문제 테스트
curl -s "http://localhost:8088/api/problems/theory?category=운영체제" | jq '.'
```

---

## ✅ 마이그레이션 완료 검증 체크리스트

### 빌드 검증
- [ ] `cd backend && ./mvnw clean compile` 성공
- [ ] `./mvnw package -DskipTests` JAR 생성 성공

### 기능 검증
- [ ] `GET /api/problems/test` → 200 OK + JSON 응답
- [ ] `GET /api/problems/1` → ProblemResponseDto 반환 (correctAnswer 필드 존재)
- [ ] `GET /api/problems/random/objective` → 리스트 반환
- [ ] `GET /api/problems/theory?category=운영체제` → 리스트 반환
- [ ] `GET /api/problems/study/meta?type=OBJECTIVE&limit=10` → TheoryProblemMetaDto 반환

### 코드 품질 검증
- [ ] ProblemResponseAssembler.java 삭제됨
- [ ] ProblemApiController가 ProblemQueryMapper 주입받음
- [ ] ProblemResponseDto.from() 메서드 존재
- [ ] 불필요한 Service 메서드 제거됨

### 프론트엔드 호환성
- [ ] React Native 앱에서 API 호출 정상 동작
- [ ] `api.ts`의 응답 타입과 실제 응답 일치
- [ ] `correctAnswer`, `isAiGenerated` 필드명 유지

---

## 🚨 롤백 계획 (문제 발생 시)

### 즉시 롤백
```bash
# Git을 사용하는 경우
cd C:\Users\SEOL\InformationExamProject
git checkout -- backend/src/main/java/com/example/informationexam/
git checkout -- backend/src/main/resources/mapper/
```

### 수동 롤백
1. Phase 4.2 되돌리기: ProblemResponseAssembler.java 파일 복원
2. Phase 2 되돌리기: ProblemApiController.java 원본으로 복원
3. Phase 1 되돌리기: ProblemResponseDto.java 원본으로 복원

---

## 📊 예상 소요 시간

| 단계 | 작업 내용 | 예상 시간 | 비고 |
|------|-----------|-----------|------|
| Phase 1 | DTO 수정 | 30분 | from() 메서드 추가 |
| Phase 2 | Controller 리팩토링 | 1시간 | Mapper 직접 호출 |
| Phase 3 | XML 최적화 | 30분 | 기존 코드 양호함 |
| Phase 4 | Service 정리 | 1시간 | Assembler 삭제 |
| Phase 5 | Stored Procedure | 1시간 | 선택사항 |
| Phase 6 | 통합 테스트 | 1시간 | API 검증 |
| **총계** | | **약 4-5시간** | |

---

## 🔧 실행 명령어 모음

```bash
# 1. 프로젝트 루트로 이동
cd C:\Users\SEOL\InformationExamProject

# 2. 백엔드 컴파일 테스트
cd backend
./mvnw clean compile

# 3. 애플리케이션 실행
./mvnw spring-boot:run

# 4. (새 터미널) API 테스트
curl http://localhost:8088/api/problems/test
curl http://localhost:8088/api/problems/1
curl http://localhost:8088/api/problems/random/objective

# 5. 프론트엔드 실행 (별도 터미널)
cd ../InformationExamApp
npm start
```

---

**최종 메시지**: 
이 계획서는 AI가 **한 번에 읽고 완벽하게 실행**할 수 있도록 모든 파일 경로, 코드 스니펫, 검증 방법을 포함하고 있습니다. 각 Phase는 독립적으로 실행 가능하며, Phase 1 → 2 → 4 순으로 진행하는 것을 권장합니다.
